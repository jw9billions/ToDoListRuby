<%= form_for [user, user.items.new] do |f| %>
  <div class='form-group'>
    <%= f.label :Item %>
    <%= f.text_field :name, class: 'form_control', placeholder: 'Enter your new item:' %>
  </div>
  <div class="form-group">
    <%= f.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>
